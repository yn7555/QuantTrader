# 历史回放模块述职汇报

## 一、模块定位

历史回放模块是 QuantFlow 量化交易平台的第6模块，核心职责是**用历史数据验证策略表现**——用户选择股票、策略、虚拟账户和时间范围，系统在历史行情上逐bar驱动策略运算，模拟撮合产生交易，最终输出完整的回测报告。

一句话概括：**输入参数，跑策略，出报告。**

---

## 二、功能清单

### 2.1 回测配置

用户在配置栏选择以下6项参数后启动回测：

| 参数 | 来源 | 说明 |
|---|---|---|
| 股票代码 | 模块1（api_data） | 可搜索输入框，支持代码/名称/拼音模糊匹配 |
| 策略 | 模块3（strategy_engine） | 下拉选择已创建的策略 |
| 虚拟账户 | 模块2（account_trading） | 下拉选择，不同账户有不同的初始资金和手续费率 |
| 时间间隔 | 本模块 | 日线/小时/分钟，决定传给策略的K线周期 |
| 起始日期 | 本模块 | 回测的时间范围 |
| 结束日期 | 本模块 | 回测的时间范围 |

### 2.2 回放播放（回测进行中）

| 功能 | 描述 |
|---|---|
| K线回放 | 逐bar播放历史K线，标注买卖信号点（红箭头买入/绿箭头卖出） |
| 播放控制 | 暂停/继续/跳转/调速 |
| 实时指标 | 8项指标卡片（总收益率、年化收益、最大回撤、夏普比率、胜率、盈亏比、交易次数、总盈亏） |
| 资金曲线 | 净值+回撤双轴图，实时更新 |
| 交易记录 | 按时间展示每笔交易的详情 |

### 2.3 回测报告（回测完成后）

回测结束后自动切换到报告视图，左侧tab导航包含5个子页面：

| Tab | 内容 |
|---|---|
| 收益概述 | 10项横向指标栏 + 策略vs基准收益对比曲线 + 每日盈亏柱状图 |
| 交易详情 | 完整交易记录表（时间/方向/股票/价格/数量/金额/手续费/盈亏/触发信号） |
| 每日持仓&收益 | 每日持仓数量、市值、浮盈、日收益率、总资产 |
| 日志输出 | 回测过程中的运行日志（INFO/WARN/ERROR） |
| 单项指标 | 预留 |

### 2.4 报告指标清单

| 指标 | 计算方式 | 含义 |
|---|---|---|
| 策略总收益率 | (期末资产/期初资产 - 1) × 100 | 总共赚了多少百分比 |
| 基准收益率 | (期末基准价/期初基准价 - 1) × 100 | 同期市场涨了多少 |
| 年化收益率 | (1+总收益)^(252/天数) - 1 | 折算成年化 |
| 最大回撤 | max((峰值-谷值)/峰值) | 最惨的时候亏了多少 |
| 夏普比率 | (均收益-无风险)/标准差 × √252 | 每单位总风险的超额收益 |
| 索提诺比率 | (均收益-无风险)/下行标准差 × √252 | 只考虑下行风险的夏普 |
| Alpha | CAPM回归 | 跑赢市场的"真本事" |
| Beta | 协方差/方差 | 对市场的敏感度 |
| 信息率 | 超额收益均值/跟踪误差 × √252 | 跑赢市场的稳定性 |
| 策略波动率 | 日收益标准差 × √252 | 收益的波动程度 |
| 基准波动率 | 基准日收益标准差 × √252 | 市场的波动程度 |
| 胜率 | 盈利卖出次数/总卖出次数 × 100 | 多少笔交易是赚钱的 |
| 盈亏比 | avg(盈利pnl)/\|avg(亏损pnl)\| | 赢的时候赢得是亏的几倍 |
| 交易次数 | 卖出订单数 | 一买一卖算一笔 |
| 总盈亏 | sum(所有卖出pnl) | 绝对盈亏金额 |

---

## 三、技术架构

### 3.1 前端

- Vite + React 19 + TypeScript 5
- Tailwind CSS 4（深色TradingView风格）
- lightweight-charts v5（K线图）+ ECharts（通用图表）
- Zustand（状态管理）
- 11个组件，2个视图（回放视图 / 报告视图）

### 3.2 后端

- Python FastAPI + SQLAlchemy 2.0 (async)
- 分层结构：router → service → models/schemas
- 17个stub接口已搭建
- 指标计算器：16个独立计算函数 + 统一入口

### 3.3 数据库（5张表）

| 表 | 用途 | 核心字段 |
|---|---|---|
| replay_session | 回测会话（参数+指标快照） | 6参数 + 17指标 + 基础信息 |
| replay_bar | 逐bar数据（行情+信号+账户） | OHLCV + benchmark_close + signal + total_assets |
| replay_order | 订单记录 | 方向/价格/数量/手续费/盈亏/信号 |
| replay_position | 持仓记录 | 股票/数量/成本/市值/浮盈 |
| replay_log | 日志条目 | 级别/内容 |

设计原则：
- 指标预存快照，跑完不可变
- 每 bar 一行，子数据拆子表
- benchmark_close 存入 bar 表，保证快照完整不依赖外部

### 3.4 模块间交互方式

**Python内部方法调用，非HTTP接口。** 所有模块跑在同一进程中，直接 import 调用。

```
from strategy_engine.service import run_backtest
result = await run_backtest(stock_code, strategy_id, account_id, timeframe, start_date, end_date)
```

---

## 四、当前进度

### 已完成

| 项目 | 状态 | 说明 |
|---|---|---|
| 前端页面 | ✅ 完成 | 配置栏 + 回放视图 + 报告视图，11个组件 |
| 后端接口 | ✅ 完成 | 17个stub接口，返回mock数据 |
| 指标计算器 | ✅ 完成 | 16个计算函数 + 统一入口，覆盖全部15项指标 |
| 数据模型 | ✅ 完成 | 5张表定义，含详细注释 |
| Mock数据 | ✅ 完成 | 逻辑自洽的模拟数据（261个交易日，5笔交易，3胜2负） |

### 未完成

| 项目 | 状态 | 说明 |
|---|---|---|
| 真实回测引擎 | ⏳ 待对接 | 需模块3提供 run_backtest 方法 |
| 数据库建表 | ⏳ 待实现 | 模型已定义，需连库后执行迁移 |
| CRUD逻辑 | ⏳ 待实现 | service层目前返回mock，需改为读写数据库 |
| 股票搜索 | ⏳ 待对接 | 需模块1提供股票搜索方法 |
| 策略列表 | ⏳ 待对接 | 需模块3提供策略列表方法 |
| 虚拟账户列表 | ⏳ 待对接 | 需模块2提供账户列表方法 |
| 回放播放控制 | ⏳ 待完善 | 前端UI已有，后端需实现暂停/继续/跳转/调速 |

---

## 五、需要其他模块的支持

### 5.1 模块1：api_data（行情数据）

| 需求 | 说明 | 优先级 |
|---|---|---|
| 股票搜索 | 输入关键词，返回匹配的股票列表（代码/名称/拼音） | 高 |
| 基准行情 | 给定时间范围，返回沪深300等指数的日线收盘价 | 高 |

调用方式：`from api_data.service import search_stocks, get_benchmark_kline`

### 5.2 模块3：strategy_engine（策略引擎）

| 需求 | 说明 | 优先级 |
|---|---|---|
| 策略列表 | 返回所有可用策略的id/名称/描述 | 高 |
| 运行回测 | **核心依赖**，传入参数，返回BacktestResult | 高 |
| 策略快照 | 获取策略的当前版本信息（用于记录"当时用的哪个版本"） | 中 |

调用方式：`from strategy_engine.service import list_strategies, run_backtest`

#### 需要模块3返回的 BacktestResult 数据结构

```
BacktestResult
├── session_id          # 会话ID
├── stock_code          # 股票代码
├── strategy_id/name    # 策略信息
├── account_id          # 虚拟账户ID
├── timeframe           # 时间间隔
├── start_date/end_date # 时间范围
├── total_bars          # 总bar数
├── time_elapsed        # 耗时
└── bars[]              # 逐bar明细
    ├── time, open, high, low, close, volume, amount  # 行情
    ├── benchmark_close                                # 基准收盘价
    ├── signal, signal_reason                          # 策略信号
    ├── orders[]                                       # 订单
    │   └── side, price, quantity, amount, commission, pnl, signal
    ├── positions[]                                    # 持仓
    │   └── stock_code, quantity, cost_price, current_price, market_value, floating_pnl
    ├── cash, total_assets                             # 账户快照
    └── log_entries[]                                  # 日志
```

### 5.3 模块2：account_trading（账户与交易）

| 需求 | 说明 | 优先级 |
|---|---|---|
| 虚拟账户列表 | 返回可用虚拟账户的id/名称/初始资金 | 中 |

调用方式：`from account_trading.service import list_virtual_accounts`

### 5.4 汇总

```
历史回放模块
    │
    ├──→ 模块1 api_data：股票搜索 + 基准行情
    │
    ├──→ 模块2 account_trading：虚拟账户列表
    │
    └──→ 模块3 strategy_engine：策略列表 + 运行回测（核心）
```

**核心阻塞项：模块3的 `run_backtest` 方法。** 此方法上线前，所有回测功能均为mock数据。

---

## 六、下一步计划

1. 对接模块3的 `run_backtest`，替换mock为真实回测
2. 连接MySQL，执行建表迁移，service层改写为读写数据库
3. 对接模块1的股票搜索和基准行情
4. 对接模块2的虚拟账户列表
5. 完善回放播放控制（暂停/继续/跳转/调速）
6. 历史回测列表页面（查看已跑过的回测）
